# Frontend Production Environment Variables
# Copy this to .env.production and fill in your values

# Backend API URL (from AWS App Runner or your backend service)
VITE_API_URL=https://your-app-runner-url.us-east-1.awsapprunner.com

# Notes:
# - This URL should point to your deployed backend service
# - Do NOT include a trailing slash
# - Get this URL from your AWS App Runner service dashboard after deployment
# - Or use API Gateway URL if using Lambda/API Gateway setup
